[build-system]
requires      = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pi_base"
version = "0.0.1"
authors = [
  { name="Ilya Ivanchenko", email="iva2k@yahoo.com" },
]
description = ""
keywords = ["raspberry pi"]
readme = "pi_base/README.md"
requires-python = ">=3.8"
license = {file = "LICENSE"}
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
# For an analysis of this field vs pip's requirements files see:
# https://packaging.python.org/discussions/install-requires-vs-requirements/
dependencies = [
]

# [project.optional-dependencies]
# dev = ["check-manifest"]
# test = ["coverage"]

[project.urls]
Homepage = "https://github.com/iva2k/pi-base"
Issues = "https://github.com/iva2k/pi-base/issues"

[project.scripts]
# pi-base = "pi_base:main"
pi-base = "Project.pi_base:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-data]
# If there are data files included in your packages that need to be installed, specify them here.
# "pi_base" = ["*"]
"pi_base.lib" = ["*"]
#"*" = ["*.*"]
# "pi_base.common" = ["*"]
# "pi_base.common.graphics" = ["*"]
# "pi_base.common.pkg" = ["*"]
# "pi_base.common.pkg.boot" = ["*"]
# "pi_base.common.pkg.etc" = ["*"]
# "pi_base.common.pkg.etc.default" = ["*"]
# "pi_base.common.pkg.etc.lightdm" = ["*"]
# "pi_base.common.pkg.etc.samba" = ["*"]
# "pi_base.common.pkg.etc.systemd" = ["*"]
# "pi_base.common.pkg.etc.systemd.system" = ["*"]
# "pi_base.common.pkg.etc.systemd.system.getty@tty1.service.d" = ["*"]
# "pi_base.common.pkg.etc.udev" = ["*"]
# "pi_base.common.pkg.etc.udev.rules*d" = ["*"]
# "pi_base.common.pkg.etc.wpa_supplicant" = ["*"]
# "pi_base.common.pkg.etc.X11" = ["*"]
# "pi_base.common.pkg.etc.X11.xorg.conf.d" = ["*"]

[tool.setuptools.packages.find]
namespaces = true
# where = ["."]
include = [
  "pi_base*",
]
exclude = [
  "pi_base.tests*",
  "pi_base.pictures*",
  "pi_base.scripts*"
]
